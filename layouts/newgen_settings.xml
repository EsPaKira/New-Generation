<container size-func="gui.get_viewport()[1],gui.get_viewport()[2]">
    <image id="background" size-func="gui.get_viewport()[1], gui.get_viewport()[2]" />
    <button hover-color="050" color="100" gravity="top-right" onclick="go_back()">@Back</button>
    <container id="settings_header" size="600,40" position-func="gui.get_viewport()[1]/2-300, 0" color="#00000050" tooltip="These settings are necessary for recalculating characters stats and for changing the gamemode" tooltip-delay="0.08">
        <label gravity="center-center">@New Generation settings</label>
    </container>

    <container position-func="gui.get_viewport()[1]/2-300, 50" size="600,2" color="#b7fcfc"></container>

    <container id="gamemodes" size="400,60" color="#00000050" position-func="gui.get_viewport()[1]/2 - 200, 150">
        <container size="160,40" pos="15,10"><label gravity="center-center">@Change gamemode</label></container>
        <select id="gamemode_select" size="140,40" onselect="function(opt) select(opt) end" pos="245,10" onclick="play_sound()">
            <option value="survival">@Survival</option>
            <option value="creative">@Creative</option>
        </select>
    </container>

    <container id="characters_recalculation" size="400,120" color="#00000050" position-func="gui.get_viewport()[1]/2 - 200, 250">
        <container size="250,40" pos="75,10" tooltip="Recalculate stats for all available characters" tooltip-delay="0.08">
            <label gravity="center-center">@Recalculate characteristics</label>
        </container>
        <button size="150,35" pos="125,75" onclick="recalculate_button()">@Recalculate</button>
    </container>

    <container id="select_bg" size="400,60" color="#00000050" position-func="gui.get_viewport()[1]/2 - 200, 410">
        <container size="160,40" pos="15,10"><label gravity="center-center">@Select background</label></container>
        <select id="bg_select" size="140,40" onselect="function(opt) select_bg(opt) end" pos="245,10" onclick="play_sound()">
            <!-- content is generated in script -->
        </select>
    </container>
</container>